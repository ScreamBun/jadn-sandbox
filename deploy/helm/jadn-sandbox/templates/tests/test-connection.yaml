apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jadn-sandbox.fullname" . }}-test-connection"
  labels:
    {{- include "jadn-sandbox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jadn-sandbox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
