apiVersion: v1
kind: Service
metadata:
  name: {{ include "jadn-sandbox.fullname" . }}
  labels:
    {{- include "jadn-sandbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "jadn-sandbox.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
